{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bonustransaction</h1>

    <table>
        <tbody>
            <tr>
                <th>Createdat</th>
                <td>{% if bonustransaction.createdat %}{{ bonustransaction.createdat|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Count</th>
                <td>{{ bonustransaction.count }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ bonustransaction.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bonustransaction_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bonustransaction_edit', { 'id': bonustransaction.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
